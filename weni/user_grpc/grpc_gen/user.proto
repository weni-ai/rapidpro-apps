syntax = "proto3";

package user;

service UserController {
    rpc List(UserListRequest) returns (stream Permission) {}
}

message UserListRequest {
    int32 user_id = 1;
    int32 org_id = 2;
}

message Permission {
    string permission = 1;
}